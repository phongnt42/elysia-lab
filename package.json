{
  "name": "app",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/app.ts & bun run --watch src/render.ts",
    "start:app": "bun run --watch src/app.ts",
    "start:render": "bun run --watch src/render.ts"
  },
  "dependencies": {
    "@bogeychan/elysia-logger": "^0.0.16",
    "app-root-path": "^3.1.0",
    "elysia": "latest",
    "fs": "^0.0.1-security"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}